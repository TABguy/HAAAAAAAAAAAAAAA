# Copy to .env and fill in if needed. NEVER commit .env.
# The leaderboard model (src/corrosion_model.py) needs NO secrets — it runs fully local.
# This file is a placeholder for any future demo-layer config (e.g. an LLM endpoint
# for the explainable pitch layer). Add keys here only if you wire one in.
