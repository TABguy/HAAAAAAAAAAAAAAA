# BACKLOG — park ideas here, don't build them mid-afternoon

Scope discipline (AGENTS.md): a narrow demo that works beats a broad one that breaks.
Freeze at 18:15. Anything not demoable goes here.

## Pre-brief decisions to lock at 13:00
- [ ] Which use case (#1 RAG / #2 triage / #3 anomaly) — see README plan.
- [ ] Confirm Bedrock model IDs in `.env` (run `python src/common.py`).
- [ ] Delete the two unused use-case stubs + Streamlit tabs to stay focused.

## Parked ideas (post-MVP only)
-
