# HAKS 2026 — Wing Corrosion. Core deps for the leaderboard model + demo layer.
# (Docling for doc parsing is a heavy optional extra -> requirements-docling.txt)

# Modelling
pandas>=2.2
numpy>=1.26
scikit-learn>=1.5

# Config + demo/pitch UI
python-dotenv>=1.0
streamlit>=1.40,<2
